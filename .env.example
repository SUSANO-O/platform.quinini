# ════════════════════════════════════════════════════════════════════════════
# MatIAsLanding — Environment Variables
#  Copy this file to .env and fill in real values.
#  NEVER commit .env to git.
# ════════════════════════════════════════════════════════════════════════════

# ── Database ──────────────────────────────────────────────────────────────────
# MongoDB Atlas connection string (database: agentflowhub_landing)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/agentflowhub_landing

# ── Auth ─────────────────────────────────────────────────────────────────────
# Random string ≥32 chars — used to sign session tokens
JWT_SECRET=change-me-to-at-least-32-random-characters

# Cifrado AES-256-GCM de secretos en reposo (tokens WhatsApp, etc.).
# En producción (Vercel) es obligatoria. Para local con la misma Mongo de prod,
# copia el mismo valor de Vercel → Settings → Environment Variables.
# Formato: 32 bytes en base64 (~44 chars) o hex (64 chars).
# Si falta, el fallback deriva de JWT_SECRET y NO descifra tokens guardados en prod.
# SECRET_ENCRYPTION_KEY=

# ── Cloudflare Turnstile CAPTCHA (Phase 3 — seguridad login/registro) ─────────
# Obtén las claves en https://dash.cloudflare.com → Turnstile → Add site
# Si alguna clave está vacía, el CAPTCHA se deshabilita (modo dev seguro).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Secret code required by /admin/promote to grant admin role
ADMIN_SECRET=change-me-to-a-strong-secret

# ── Acceso por invitación ─────────────────────────────────────────────────────
# Códigos de autorización para el registro. Formato: CODIGO:plan o CODIGO:plan:diasPrueba
# Se validan en POST /api/auth junto con los códigos del admin (MongoDB).
# Planes válidos: free | solo | basic | team | plus | starter | growth | business | enterprise
# Ejemplo: REGISTRATION_CODES=TAM1:team:30,BETA-BASIC:basic:15,VIP-STARTER:starter
REGISTRATION_CODES=

# ── App URL ───────────────────────────────────────────────────────────────────
# Public URL of this app (no trailing slash)
# Origen público de la app (landing + dashboard). Sin barra final.
NEXT_PUBLIC_APP_URL=https://botiva.space
# SDK embebido en marketing/dashboard: por defecto {NEXT_PUBLIC_APP_URL}/widget.js
# NEXT_PUBLIC_LANDING_WIDGET_SCRIPT_URL=
# Asistente interno de plataforma (dashboard/marketing): {NEXT_PUBLIC_APP_URL}/assist.js
# NEXT_PUBLIC_ASSIST_SCRIPT_URL=

# ── Asistente interno (boot /api/internal/assist/boot) ───────────────────────
# INTERNAL_APP_ASSIST_AGENT_ID=math-ais
# INTERNAL_APP_ASSIST_COLOR=#fb0e0e
# INTERNAL_APP_ASSIST_TITLE=Math-ais
# INTERNAL_APP_ASSIST_FAB_HINT=¿tienes duda en el uso?
# INTERNAL_MARKETING_ASSIST_AGENT_ID=math
# Widget MongoDB del asistente Math (capturas, handoff). Crear en Mis widgets y pegar el _id aquí.
# INTERNAL_MARKETING_ASSIST_WIDGET_ID=
# INTERNAL_APP_ASSIST_WIDGET_ID=
# INTERNAL_MARKETING_ASSIST_COLOR=#f5540f
# INTERNAL_ASSIST_EDGE_INSET=20
# INTERNAL_ASSIST_OFFSET_BOTTOM=20
# INTERNAL_ASSIST_HUMAN_PHONE=+57 3196748729
# Email admin dueño de Math / Math-ais (cuenta plataforma)
# INTERNAL_ASSIST_OWNER_EMAIL=admin@agentflowhub.com
# MCP Mongo solo lectura para Math-ais (alternativa: UI en /admin/landing-assist)
# ASSIST_MONGO_URI=mongodb+srv://...@cluster.../agentfarm

# ── Stripe ────────────────────────────────────────────────────────────────────
# Stripe secret key (sk_test_* for test, sk_live_* for production)
STRIPE_SECRET_KEY=sk_test_...

# Clave publicable (pk_test_* / pk_live_*) — misma cuenta que STRIPE_SECRET_KEY.
# Necesaria para Stripe Elements (cambiar tarjeta sin salir de la app).
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Webhook signing secret (cada endpoint tiene su propio whsec_...)
# - Producción / Vercel: npm run stripe:create-webhook -- https://tu-dominio.com
# - Local: crea .env.local con STRIPE_WEBHOOK_SECRET = salida de `npm run stripe:print-secret`
#          y en otra terminal: npm run stripe:listen (reenvío a localhost:3201)
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_WEBHOOK_PUBLIC_URL=https://tu-landing.vercel.app

# Stripe Price IDs for each plan (Dashboard → Products → Price → copy price_...)
# Deben ser del MISMO modo que STRIPE_SECRET_KEY (test con sk_test_*, live con sk_live_*).
STRIPE_PRICE_STARTER=price_...
STRIPE_PRICE_GROWTH=price_...
STRIPE_PRICE_BUSINESS=price_...

# ── Email (Resend) ────────────────────────────────────────────────────────────
# Get your API key at https://resend.com
RESEND_API_KEY=re_...

# From address: the DOMAIN part must be added + verified in https://resend.com/domains
# (DNS en tu proveedor; no hace falta que el servidor sea “local” o público).
# En local: usa el mismo dominio verificado, u otro que ya tengas en Resend.
# Si RESEND_API_KEY está vacío, los correos solo se loguean en consola (sin envío real).
EMAIL_FROM=BotIvA<noreply@botiva.space>

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins for Widget API requests
# Leave empty to allow all origins (not recommended for production)
# Para pruebas locales del embed (PHP en http://localhost:9090, etc.) localhost
# se acepta siempre en /api/widget/* — no hace falta listarlo aquí.
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ── LemonSqueezy (alternativa a Stripe) ─────────────────────────────────────
# Solo necesario si usas el flujo de checkout con LemonSqueezy.
# Dashboard → Settings → API → API Keys
LEMONSQUEEZY_API_KEY=
# Dashboard → Settings → Store → Store ID
LEMONSQUEEZY_STORE_ID=
# Dashboard → Settings → Webhooks → signing secret
LEMONSQUEEZY_WEBHOOK_SECRET=
# Dashboard → Products → Variant IDs de cada plan (precios en src/lib/plan-catalog.ts)
LEMONSQUEEZY_VARIANT_SOLO=
LEMONSQUEEZY_VARIANT_API_DEVELOP=
LEMONSQUEEZY_VARIANT_TEAM=
LEMONSQUEEZY_VARIANT_PLUS=
LEMONSQUEEZY_VARIANT_BUSINESS=
# Legacy (solo webhooks de suscripciones existentes — Basic/Starter/Growth retirados de venta)
LEMONSQUEEZY_VARIANT_BASIC=
LEMONSQUEEZY_VARIANT_STARTER=
LEMONSQUEEZY_VARIANT_GROWTH=
# Packs de conversaciones one-time ($15 / $60 / $170 — solo planes de pago)
LEMONSQUEEZY_VARIANT_PACK_S=
LEMONSQUEEZY_VARIANT_PACK_M=
LEMONSQUEEZY_VARIANT_PACK_L=
# TRM COP/USD para scripts de LemonSqueezy (default 4200)
LEMONSQUEEZY_TRM_COP=4200

# Emisor en PDFs de facturas manuales (/dashboard/facturas)
BILLING_ISSUER_NAME=BotIvA
BILLING_ISSUER_TAX_ID=
BILLING_ISSUER_ADDRESS=
BILLING_ISSUER_CITY=
BILLING_ISSUER_STATE=
BILLING_ISSUER_PHONE=
BILLING_ISSUER_EMAIL=business.botiva@gmail.com

# ── WhatsApp alertas de handoff (opcional; desactivado por defecto) ───────────
# Las plantillas Meta tienen costo. Por defecto usamos texto libre (gratis) si hay
# ventana 24 h: el dueño debe haber escrito al número Business recientemente.
# HANDOFF_WA_TEMPLATE_ENABLED=1
# HANDOFF_WA_TEMPLATE_NAME=botiva_handoff
# HANDOFF_WA_TEMPLATE_LANG=es

# ── Pollinations (Generar avatar AI en widget builder / AvatarEditor) ───────
# Clave en enter.pollinations.ai — sin esto, «Generar AI» devuelve error claro (402 si falta saldo).
# POLLINATIONS_API_KEY=

# ── Cloudinary (capturas del widget chat) ────────────────────────────────────
# Dashboard Cloudinary → Account Details → API Key, API Secret, Cloud name
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
# Carpeta raíz en Cloudinary (opcional; default: botiva/widget-chat)
CLOUDINARY_FOLDER=botiva/widget-chat

# En producción (Vercel) se usa Jina AI Reader — gratis, sin cuenta, sin API key.
# En desarrollo local se usa Puppeteer con Chrome del sistema (auto-detectado).
# Solo define esto si Chrome no está en la ruta estándar:
# PUPPETEER_EXECUTABLE_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe

# ── RAG / embeddings en la landing (procesamiento de documentos con Gemini) ─
# Misma familia de clave que uses en el hub si centralizas; obligatoria si usas RAG en esta app.
GEMINI_API_KEY=
# Gemini API key con acceso a modelos 2.x, 3.x, Deep Research, Gemma 4, Lyria, Robotics (33 modelos)
VERTEX_GEMINI_API_KEY=

# ── Subida de archivos RAG (Vercel Blob) ─────────────────────────────────────
# En Vercel: Storage → Blob → Connect to project (inyecta BLOB_READ_WRITE_TOKEN).
# Permite PDFs/archivos hasta 10 MB evitando el límite 4.5 MB de las funciones serverless.
# Sin esto, la subida directa queda limitada a ~4 MB en producción Vercel.
BLOB_READ_WRITE_TOKEN=

# ── Jobs internos (p. ej. Vercel Cron → /api/internal/subscription-reminders) ─
# Sin esto, ese endpoint responde 503. Valor compartido con la cabecera o query ?secret= del cron.
CRON_SECRET=

# ── Smoke / E2E dev (scripts/webhook-gating-smoke.mjs) ───────────────────────
# URL HTTPS de echo para probar webhooks salientes (MatIAs → tu backend).
# Por defecto usa webhook.site del equipo; puedes crear la tuya en https://webhook.site
TEST_WEBHOOK_ECHO_URL=https://webhook.site/68c7194c-8b87-402b-8641-9c8fd8e7240a

# ── URLs solo servidor (emails: enlaces y assets) ───────────────────────────
# Origen absoluto para enlaces en correo (si difiere del dominio público).
APP_URL=https://botiva.space
# Origen público solo para imágenes en HTML de email (logo); por defecto NEXT_PUBLIC_APP_URL.
EMAIL_PUBLIC_ASSET_BASE=https://botiva.space
# Opcional: GIF celebración en /dashboard tras suscripción (URL absoluta o relativa si sirves el asset).
NEXT_PUBLIC_SUBSCRIPTION_HERO_GIF=

# ── Finanzas internas (panel /dashboard/finance) ────────────────────────────
# Fallback global si no se puede clasificar modelo por widget/agente.
FINANCE_EST_USD_PER_MESSAGE=0.006
# Coste por mensaje para modelos económicos (flash/mini/small).
FINANCE_EST_USD_PER_MESSAGE_FLASH=0.001
# Coste por mensaje para modelos premium (pro/ultra/claude/gpt-4+).
FINANCE_EST_USD_PER_MESSAGE_PREMIUM=0.017
# Blend observado factura GCP may 2026 (~$48.78 / ~13.9M tokens).
FINANCE_INVOICE_USD_PER_1M=3.51
# Multiplicador cuando el agente tiene RAG habilitado (prompt más largo con chunks).
FINANCE_EST_RAG_MULTIPLIER=2.0
# Coste fijo por mensaje RAG: embedding query (Gemini) + búsqueda Pinecone (tier de pago).
FINANCE_EST_RAG_VECTOR_USD=0.00008
# ── Infra externa mínima (sin tiers gratis) — plan-economics + pricing-audit ─
# MongoDB Atlas M10 mínimo (~$57/mes). No usar M0 en proyecciones.
FINANCE_EST_MONGO_CLUSTER_USD=57
# Storage adicional Atlas (~$0.25/GB/mes).
FINANCE_EST_MONGO_STORAGE_USD_GB=0.25
# Pinecone Builder / mínimo serverless de pago (~$20/mes).
FINANCE_EST_PINECONE_MIN_USD=20
# Storage Pinecone serverless (~$0.33/GB/mes).
FINANCE_EST_PINECONE_STORAGE_USD_GB=0.33
# Vectores Pinecone ≈ % del volumen de texto RAG en Mongo.
FINANCE_EST_PINECONE_VECTOR_RATIO=0.08
# Overhead duplicar índice Mongo + Pinecone.
FINANCE_EST_VECTOR_DUP_OVERHEAD_PCT=0.12
# Clientes de pago para repartir cluster (conservador: pocos = más $/cliente).
FINANCE_EST_PAYING_CUSTOMERS_BASE=25
# % de cuota RAG usada en proyección (0.4 = 40%).
FINANCE_EST_RAG_STORAGE_FILL=0.4
# Escrituras Mongo: logs, sesiones, widgets (~$0.000012/conv).
FINANCE_EST_MONGO_OPS_USD_CONV=0.000012
# Factor historial largo → storage operacional.
FINANCE_EST_HISTORY_STORAGE_FACTOR=0.000004

# ── AgentFlowHub / AIBackHub (sync agentes, widget-events, admin Embeddings 3D) ─
# URL base del hub (servidor Express). Debe coincidir con BACKEND_URL en AgentFlowhub.
BACKEND_URL=http://127.0.0.1:9003
# Alias opcional: misma URL si ya usas AUTH_BACKEND_URL en otro despliegue.
AUTH_BACKEND_URL=

# Debe coincidir con API_KEY en AIBackHub/.env (cabecera x-api-key). Vacío solo si el hub no define API_KEY (dev abierto).
AIBACKHUB_API_KEY=
# Identificador de tenant en Mongo del hub (cabecera x-tenant-id). Local típico: default
AIBACKHUB_TENANT_ID=default
# Opcional: si AIBackHub define ADMIN_API_KEY y las rutas /api/admin/* no aceptan solo x-api-key,
# pon aquí la misma clave (la landing la envía como x-admin-key al proxy de /api/admin/embedding-viz).
AIBACKHUB_ADMIN_KEY=

# Secreto compartido: AIBackHub llama a POST /api/internal/sync-from-hub tras editar un agente
# enlazado desde la landing (misma cadena en el hub: HUB_TO_LANDING_SECRET).
# También se usa para firmar HMAC en la comunicación landing ↔ AgentFlowhub (OBLIGATORIO en prod).
# Genera con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
HUB_TO_LANDING_SECRET=

# ── Rate limiting distribuido (Redis/Upstash) ────────────────────────────────
# Sin estas vars el rate limiter cae a in-memory (solo válido para instancia única).
# En multi-instancia (Vercel, Railway con >1 réplica) DEBES configurar Redis.
# Crea una BD gratuita en https://upstash.com → copia REST URL y token.
REDIS_URL=https://xxx.upstash.io
REDIS_TOKEN=AX...
# Chat widget (por minuto). Defaults: 240 por IP, 96 por IP+agente.
# WIDGET_CHAT_RATE_LIMIT_PER_MINUTE=240
# WIDGET_CHAT_PER_AGENT_RATE_LIMIT_PER_MINUTE=96
# Número de proxies de confianza delante de la app (Vercel/Cloudflare/Railway = 1).
# Ajusta si tu infra tiene más de un proxy. 0 = sin proxy (acceso directo).
TRUSTED_PROXY_COUNT=1

# Opcional: append JSONL de POST /api/widget/events a `widget-usage.jsonl` (solo depuración).
# En Vercel/Lambda el archivo va bajo el tmp del runtime, no bajo /var/task.
# WIDGET_USAGE_DISK_LOG=1

# ── WebPush / Push Notifications (VAPID) ────────────────────────────────────
# Genera el par de claves con: npx web-push generate-vapid-keys
# VAPID_EMAIL: cualquier email tuyo (se envía en el header de VAPID).
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:admin@yourdomain.com

# ── Sentry (opcional — monitoreo de errores) ────────────────────────────────
# Si no instalas @sentry/nextjs, este valor se ignora silenciosamente.
# npm install @sentry/nextjs  →  https://sentry.io → nuevo proyecto Next.js → copiar DSN
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=

# ── AgentFlowhub (Next — proxy de /api/widget/chat desde la landing) ──────────
# El SDK embebido usa host = tu landing; el chat se reenvía al hub para resolver agentes/RAG.
# Sin AgentFlowhub en marcha, Preview/widget devuelve 502 al enviar mensajes.
# En la carpeta AgentFlowhub: npx next dev -p 9002 (o el puerto que pongas aquí).
# AgentFlowhub necesita BACKEND_URL apuntando a AIBackHub (p. ej. 9003).
# Para validar tokens wt_* guardados en Mongo (Mis widgets), en el .env de AgentFlowhub
# pon HUB_TO_LANDING_SECRET igual que arriba y LANDING_INTERNAL_URL a la URL base de esta
# landing (p. ej. http://127.0.0.1:3201).
AGENTFLOWHUB_URL=http://127.0.0.1:9002

# URL pública del UI AgentFlowhub (enlaces «Abrir MCP», Panel Admin → Supervisión → «Abrir granja en Hub»).
# Debe coincidir con el origen donde corre AgentFlowhub en el navegador (no con AGENTFLOWHUB_URL del proxy si difieren).
# Por defecto local: 9010. Si no defines esta variable, el admin usa el mismo fallback que el dashboard MCP.
NEXT_PUBLIC_AGENTFLOWHUB_URL=http://127.0.0.1:9010

# ── MCP estándar (landing — formulario «Conectar integración MCP») ───────────
# URL por defecto del campo «URL del servidor MCP» cuando el usuario elige
# «MCP Server (estándar)» (integrationKey mcp_standard). Debe ser http:// o https://
# accesible desde AIBackHub (BACKEND_URL), no solo desde el navegador.
# Ejemplo local: http://127.0.0.1:8080/mcp
# Déjalo vacío si no quieres pre-rellenar.
NEXT_PUBLIC_MCP_STANDARD_DEFAULT_URL=

# Auditoría fases MCP (A–E) + curl al hub: npm run verify:mcp-phases

# ── Agent Flow API (documentación REST — plan Team+) ─────────────────────────
# URL pública del servicio agent-flow-api (docs + /api/v1).
# Opcional: si no se define, la landing elige sola — local (:4000) en localhost y Cloud Run en producción.
# Usa 127.0.0.1 en local (no localhost): en Windows localhost→::1 puede apuntar a un API viejo en Docker.
# NEXT_PUBLIC_AGENTFLOW_API_URL=http://127.0.0.1:4000

# ════════════════════════════════════════════════════════════════════════════
#  STRIPE + MONGODB — QUÉ REVISAR SI EL PLAN NO SE ACTUALIZA
# ════════════════════════════════════════════════════════════════════════════
#
#  Stripe Dashboard (mismo modo test/live que tu STRIPE_SECRET_KEY)
#  ───────────────────────────────────────────────────────────────────────────
#  • Suscripciones → abre la suscripción → el Price debe ser el que configuraste.
#  • En la suscripción, Metadata: debería incluir plan=starter|growth|business
#    (los nuevos checkouts lo envían vía subscription_data.metadata; suscripciones
#    antiguas pueden no tenerlo hasta que renueves o lo añadas a mano en Stripe).
#  • Developers → Webhooks → endpoint https://TU_DOMINIO/api/webhooks/stripe
#    → el "Signing secret" (whsec_...) es el que va en STRIPE_WEBHOOK_SECRET.
#  • En local: stripe listen --forward-to localhost:PUERTO/api/webhooks/stripe
#    y pega el whsec que muestra la CLI en STRIPE_WEBHOOK_SECRET (no el de producción).
#
#  Customer Portal (opcional — solo si usas “Abrir portal avanzado de Stripe” en Ajustes)
#  ───────────────────────────────────────────────────────────────────────────
#  Tarjeta y facturas in-app: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY + /api/billing/setup-intent e invoices.
#  Si quieres el portal hosted: Stripe Dashboard → Billing → Customer portal → activar y guardar.
#
#  Variables .env (deben ser coherentes entre sí)
#  ───────────────────────────────────────────────────────────────────────────
#  • STRIPE_PRICE_STARTER (etc.) = Price ID exacto (price_...) del producto en Stripe.
#  • Si el price_... no coincide con la suscripción real, el código puede inferir
#    el plan por importe según plan-catalog.ts: 7 / 29 / 42 / 749 USD (venta)
#    → unit_amount 700, 2900, 4200, 74900 (centavos).
#  • Producción COP: checkout vía LemonSqueezy (LEMONSQUEEZY_VARIANT_*).
#
#  MongoDB — colección subscriptions (modelo Subscription)
#  ───────────────────────────────────────────────────────────────────────────
#  • userId: mismo string que el _id del usuario en users (sesión).
#  • stripeCustomerId / stripeSubscriptionId: rellenos tras checkout o sync.
#  • plan: free | solo | basic | plus | starter | growth | business | enterprise
#  • status, currentPeriodEnd (epoch segundos, fin de periodo actual en Stripe).
#
# ════════════════════════════════════════════════════════════════════════════
#  PRODUCTION CHECKLIST
# ════════════════════════════════════════════════════════════════════════════
#  SEGURIDAD
#  [ ] Generar JWT_SECRET aleatorio ≥32 chars (node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
#  [ ] Generar HUB_TO_LANDING_SECRET aleatorio ≥32 chars (mismo en AgentFlowhub/.env)
#  [ ] Generar par VAPID: npx web-push generate-vapid-keys
#  [ ] Configurar REDIS_URL + REDIS_TOKEN (Upstash gratuito) para rate limiting distribuido
#  [ ] Establecer TRUSTED_PROXY_COUNT=1 (Vercel/Cloudflare/Railway) o ajustar según infra
#  [ ] Configurar allowedOrigins por widget desde /api/widgets/[id]/allowed-origins
#
#  PAGOS
#  [ ] Usar live Stripe keys (sk_live_*) y live webhook signing secret (whsec_*)
#  [ ] Registrar webhook de Stripe → https://YOUR_DOMAIN/api/webhooks/stripe
#
#  APP
#  [ ] Set NEXT_PUBLIC_APP_URL al dominio de producción (sin trailing slash)
#  [ ] Verificar dominio EMAIL_FROM en Resend
#  [ ] Configurar AGENTFLOWHUB_URL a la URL pública de AgentFlowhub en producción
#  [ ] Rotar ADMIN_SECRET antes de deploy
#      Events to enable (must match route handlers):
#        - checkout.session.completed
#        - customer.subscription.created
#        - customer.subscription.updated
#        - customer.subscription.deleted
#        - invoice.paid          (email con factura PDF: alta, cambio de plan, renovación)
#        - invoice.payment_failed
#        - customer.subscription.trial_will_end
#fix