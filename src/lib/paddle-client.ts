'use client';

/**
 * Paddle.js (cliente) — COMENTADO. Migrado a LemonSqueezy.
 * LemonSqueezy no requiere JS de cliente — usa checkout hospedado vía redirect.
 * → Ver git history para la implementación completa de Paddle.
 */

// export function initPaddleClient(): Promise<void> { ... }
